services:
  db:
    image: pgvector/pgvector:pg16
    environment:
      POSTGRES_USER: filingsage
      POSTGRES_PASSWORD: dev
      POSTGRES_DB: filingsage
    ports:
      - "5433:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data
volumes:
  pgdata:
